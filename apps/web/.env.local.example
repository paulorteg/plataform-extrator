# MercadoIA BO Platform web local preview
#
# Copy this file to .env.local and fill only public frontend values.
# Never place service role, database URL, JWT secret, refresh token, or real backend secrets here.

VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
